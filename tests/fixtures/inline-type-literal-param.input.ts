export function createUser(opts: { name: string, age: number }): void {
  const n: string = opts.name;
}

export function greet(greeting: string, opts: { loud: boolean }): void {
  const l: boolean = opts.loud;
}
