---
source: tests/integration_test.rs
expression: output
---
use regex::Regex;

fn makePattern() -> f64 {
    let pattern = Regex::new(r"hello").unwrap();
    let count: f64 = 1.0;
    count
}

fn makeInsensitivePattern() -> f64 {
    let pattern = Regex::new(r"(?i)hello").unwrap();
    let count: f64 = 2.0;
    count
}
