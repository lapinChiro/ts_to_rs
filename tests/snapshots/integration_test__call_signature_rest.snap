---
source: tests/integration_test.rs
expression: output
---
trait VarargHandler {
    fn call_0(&self, args: Vec<f64>);
}

trait Formatter {
    fn call_0(&self, template: String, values: Vec<f64>) -> String;
}

trait Callback {
    fn call_0(&self, x: f64, y: f64) -> f64;
}
