function main(): void {
    const name: string = "World";
    const n: number = 42;
    console.log(`Hello ${name}`);
    console.log(`The answer is ${n}`);
    console.log(`${n} + ${n} = ${n + n}`);
}
