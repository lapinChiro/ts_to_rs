[package]
name = "compile-check"
version = "0.0.0"
edition = "2021"

[dependencies]
scopeguard = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
