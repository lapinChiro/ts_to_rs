# TODO

PRD 化前のイシュー一覧。優先度は `todo-prioritization.md` の L1〜L4 レベルで決定し、Tier に反映。
Tier 内の記載順序が優先度。根本原因を共有するイシューはクラスタとしてバッチ対応する。

- 各項目には **安定 ID**（`I-01` 等）を付与。新規追加時は最終 ID + 1 を採番
- 件数は Hono ベンチマーク実測値（最終実行: 2026-04-05、112/158 クリーン、56 エラー、hono_sha: f82aba8）
---

## Tier 0: L1（信頼性基盤）+ L2（設計基盤）

### ~~RC-15: 型変換シグネチャの設計問題 [L2]~~ **Batch 10 で完了**

~~`convert_ts_type` に型パラメータコンテキストがなく、型パラメータ制約の解決が不完全。~~

- ~~**[I-299]** `convert_ts_type` に型パラメータコンテキスト導入~~ — SyntheticTypeRegistry.type_param_scope で合成型に伝播
- ~~**[I-273]** trait/struct 混同（E0404）~~ — モノモーフィゼーションで解決。非 trait 制約を具象型で置換。ビルトイン外部型の残存は I-369 で修正済み

---

## Tier 1: L3（拡大する技術的負債・ブロッカー）

### RC-9: 型引数推論のフィードバック欠如

- **[I-311]** **推論結果の引数 expected type フィードバック** — `fn<T>(x: T, y: T)` で推論結果を未解決引数に反映する 2 パスが必要。`src/pipeline/type_resolver/call_resolution.rs:119-125`。解決で I-344 が自動解消
- **[I-344]** **generic 関数への文字列リテラルで型不一致** — 🔗 I-311 解決で自動解消の可能性

### RC-11: expected type 伝播の不完全性

- **[I-362]** **`propagate_destructuring_defaults` が KeyValue/ネストパターン未対応** — Object の `ObjectPatProp::Assign` と Array の `Pat::Assign` は対応済（I-361 で追加）。未対応: (1) KeyValue のデフォルト値（`{ color: c = "black" }`）— `ObjectPatProp::KeyValue` の `kv.value` が `Pat::Assign` の場合を処理していない。(2) ネストパターン（`{ a: { b = 5 } }`）— `ObjectPatProp::KeyValue` の `kv.value` が `Pat::Object` の場合に再帰しない。🔗 I-361 の変数型登録とは独立した責務（expected type 伝播）
- **[I-300]** **imported 関数の expected type 伝播不足** — OBJECT_LITERAL_NO_TYPE の一部。directory モードでの TypeRegistry 情報共有が必要
- **[I-301]** **型注釈なしオブジェクトリテラル** — OBJECT_LITERAL_NO_TYPE の一部。匿名構造体自動生成
- **[I-306]** **`.map()` callback 戻り値型の伝播** — OBJECT_LITERAL_NO_TYPE の一部。`propagate_expected` が `Expr::Call` 未処理

OBJECT_LITERAL_NO_TYPE 全体で 25件。I-300/I-301/I-306 のサブ分類件数は要再調査。

### RC-13: union/enum 型の生成品質

- **[I-238]** **合成 enum の Display trait 未生成** — コンパイルテスト skip 原因
- **[I-202]** **Box\<dyn Fn\> を含む enum の derive 不適合** — skip 原因
- **[I-11]** **三項演算子の分岐値を enum variant ラップ** — skip 原因（ternary, ternary-union）

### RC-2 残り: 所有権・借用モデル

- **[I-340]** **Generic に Clone bound 未付与** — body 解析で `T: Clone` 自動付与が必要
- **[I-217]** **filter/find クロージャの参照型ミスマッチ** — `&f64` vs `f64`
- **[I-265]** **find の Option 二重ラップ**

### RC-12: string メソッドマッピング

- **[I-329]** **charAt/repeat 変換未実装** — indexOf は実装済み。charAt・repeat の個別マッピング追加が残存
- **[I-237]** **toFixed 未変換** — `format!("{:.N}", v)` に変換。skip 原因

### RC-5: abstract class 変換パスの欠陥

- **[I-336]** **super() がリテラル出力** — `generate_child_of_abstract()` で `rewrite_super_constructor()` 未呼出
- **[I-337]** **trait default method でフィールドアクセス不可** — concrete メソッドの一律 trait 化。I-336 と強依存

### ~~RC-16: Hono types.rs の dir compile 阻害~~ **Batch 11a で I-368+I-369 完了**

- ~~**[I-368]** OutputWriter の `types.rs` パス衝突~~ — 共有合成型モジュールを `shared_types.rs` に衝突回避。dir compile 156→157
- ~~**[I-369]** ビルトイン外部型のモノモーフィゼーション未適用~~ — `generate_external_struct` にモノモーフィゼーション適用 + `base_synthetic` 伝播
- **[I-370]** **`Input['out']` の indexed access 解決失敗（E0405）** — `lookup_field_type`（`indexed_access.rs:271`）で `Input` の `out` フィールドが解決できず `Input::out` という修飾パスがそのまま出力される。モノモーフィゼーションとは別系統。Hono types.rs で 1 件

### ゲートイシュー・ブロッカー

- **[I-30]** **Cargo.toml 依存クレート追加** — I-183（fetch）と I-34（Math.random）のゲート
- **[I-182]** **Hono クリーンファイルのコンパイルテスト CI 化** — 回帰検出自動化
- **[I-313]** **三項演算子 callee パターン** — CALL_TARGET（4件）。一時変数束縛 + 呼び出し

---

## Tier 2: L3 残り + L4（修正コスト小）

### 独立コンパイルエラー

- **[I-321]** **クロージャ返却の Box::new ラップ漏れ** — `needs_trait_box_coercion` のカバー不足
- **[I-322]** **DU switch のデストラクチャ変数 rewrite 不完全** — `is_du_field_binding` スコープ検出
- **[I-326]** **`??` チェーンの型不一致** — ネスト `??` の中間結果型
- **[I-330]** **try/catch + return で unreachable code** — ブロック構造見直し
- **[I-360]** **Option\<T\> 変数の truthy narrowing 未対応** — `if (x)` where `x: Option<T>` が `if x {` のまま生成される。`if let Some(x) = x` に変換すべき。`try_generate_narrowing_match`（`src/transformer/statements/control_flow.rs:250`）が `complement_pattern == "None" && is_swap` のみ処理し、truthy（`is_swap=false`）パスが未処理。fixture `functions` のコンパイルを阻害
- **[I-331]** **Option\<T\> 戻り値の暗黙 None 未返却** — if else 欠落
- **[I-332]** **as unknown as T 二段キャスト** — 中間 unknown 消失
- **[I-310]** **HashMap computed access が `as usize`** — Vec 用変換の誤適用。transformer 側修正
- **[I-314]** **intersection の未使用型パラメータ（E0091）** — skip 原因
- **[I-201]** **null as any の非 Option 代入** — skip 原因
- **[I-209]** **any-narrowing enum の値代入型強制** — skip 原因
- **[I-345]** **Tuple 配列デストラクチャリングが `tuple.get(idx).cloned().unwrap()` を生成** — `try_convert_array_destructuring`（`src/transformer/statements/destructuring.rs:155`）が source の型を見ずに `build_safe_index_expr_unwrapped` を生成。Rust Tuple は `tuple.0` 構文であり `tuple.get(0).cloned().unwrap()` はコンパイルエラー。解決策: source 型が `RustType::Tuple` の場合、`Expr::FieldAccess { field: "0" }` を生成（`convert_member_expr` の Tuple 分岐と同じパターン）

### 型変換・推論

- **[I-324]** **文字列結合の &str/String 不一致** — `src/transformer/expressions/binary.rs:75-143` が全ケース未網羅。現時点で Hono ベンチマーク/fixture 双方で実質影響なし
- **[I-309]** **メソッド呼び出しの型引数推論** — Member callee 未処理。影響限定的
- **[I-101]** **ジェネリック型を含む intersection** — `try_convert_intersection_type` と統合可能
- **[I-278]** **new obj.Class() パターン** — `resolve_new_expr` が `Expr::Member` 未処理
- **[I-181]** **call signature のジェネリック型パラメータ** — 制約伝搬未検証
- **[I-284]** **typeof qualified name（typeof A.B.C）** — 0 エラー削減。高コスト・低 ROI
- **[I-296]** **intersection 内の複数 union 分配** — 意図的制約。需要発生時に対応

### 設計・品質

- **[I-359]** **`register_union` の単一メンバー dedup 時に 1-variant enum が生成される** — `src/pipeline/synthetic_registry.rs:90-98`。variant 名 dedup で 2→1 になった場合、1-variant enum が登録される。呼び出し元が `RustType::Named` でラップするため冗長な enum になる。修正には `register_union` の返り値型変更（`String` → `enum { Single(RustType), Union(String) }`）が必要で全 7 呼び出し元に影響。実発生確率は極めて低い（`Fn` 型で params が異なり return_type が同一のケースのみ）
- **[I-358]** **カウンタベース命名の順序依存性** — `src/pipeline/synthetic_registry.rs:27-30`。`struct_counter` / `synthetic_counter` の値はファイル処理順に依存。dedup ヒット時は同一名を返すため構造的同値性は保たれるが、初回登録の名前（`_TypeLit{N}`, `_Intersection{N}`）がビルドごとに異なりうる。現時点で実害なし。incremental compilation やキャッシュ導入時に問題化する可能性
- **[I-193]** **API 表面積の最小化** — `pub fn` → `pub(crate) fn`
- **[I-104]** **所有権推論（全体設計）** — 全 clone → ライフタイム分析。RC-2 の根本解決
- **[I-103]** **エラー型戦略** — String 固定 → thiserror/anyhow
- **[I-102]** **any/unknown の残課題** — typeof/instanceof 未使用の any は Value のまま
- **[I-274]** **フィールド名の IR サニタイズ不一致** — I-270c で顕在化見込み
- **[I-279]** **resolve_synthetic_placement の O(n²)** — 大規模プロジェクトでボトルネック
- **[I-371]** **合成型の単一正準配置欠如** — 合成型が複数の Rust モジュールに独立して定義される設計欠陥。同一ファイル内重複（E0428）+ クロスファイル冗長性 + shared_types.rs インポート欠如の 3 症状。Hono types.rs で E0428×5 + E0119×12 = 17 エラー。設計レベルの修正が必要（パイプラインの per-file loop + OutputWriter + 単一ファイル API の連動変更）。PRD → `backlog/i371-synthetic-type-canonical-placement.md`
- **[I-62]** **is_string_type の Option\<String\> ハック除去**
- **[I-293]** **全ファイルのテストケース体系的レビュー**

### セマンティクス・解析精度

- **[I-342]** **getter/setter 呼び出し側がフィールドアクセスのまま** — ClassInfo に getter 一覧保持 → MethodCall 生成
- **[I-364]** **rest デストラクチャ変数の型が Unknown** — I-361 で rest 変数のスコープ登録は修正済み（以前は未登録）。残課題: `register_pat_vars` の `ObjectPatProp::Rest` 分岐（`src/pipeline/type_resolver/visitors.rs:347-351`）が source_type に `None` を渡すため `Unknown` で登録される。正確な型は source type から明示フィールドを除いた残余構造体型であるべき。解決策: source type の `TypeDef::Struct` からフィールド一覧を取得し、同じ Object パターン内の明示フィールド名を除外した残余フィールドで新しい構造体型を構築する。`expand_rest_as_synthetic_struct`（`src/transformer/functions/destructuring.rs:232`）に類似ロジックがある
- **[I-363]** **const デストラクチャの mutable 判定に is_object_type チェックがない** — `visit_var_decl` の Ident パス（`src/pipeline/type_resolver/visitors.rs:246`）では `is_const && is_object_type(&var_type)` で const オブジェクト型変数を mutable にする処理があるが、デストラクチャリングパスの `register_pat_vars` → `declare_var` では `!is_const` を固定で渡すだけ。`const { inner }: Outer = obj;` で `inner` が Named 型の場合、TS では const でもフィールド変更可能だが、生成 Rust で `let inner` になり `inner.field = x` がコンパイルエラーになる。解決策: `register_pat_vars` の `declare_var` 呼び出しで `is_const && is_object_type` を評価する。ただし `register_pat_vars` に `source_type` が渡るようになった（I-361）ため、型情報は利用可能
- **[I-260]** **undefined リテラルの型解決が Unknown**
- **[I-188]** **substring/slice のバイト境界安全性** — UTF-16 vs バイト

### ビルトイン型拡張

- **[I-261]** **ES2016〜ES2024 の追加メソッド** — libMap 拡張
- **[I-263]** **型エイリアス（alias）の TypeRegistry 登録**
- **[I-270c]** **ビルトイン型のメソッド impl 生成** — 🔗 I-274
- **[I-262]** **Intl 名前空間** — 需要発生時
- **[I-264]** **Proxy/Reflect** — 需要発生時

---

## Tier 3: L4（局所的・孤立した問題）

### 未対応構文（小修正）

- **[I-196]** parseInt の radix 引数 — 2 件
- **[I-12]** Promise\<T\> の union 内展開
- **[I-13]** super() の位置ベースフィールドマッピング
- **[I-21]** async void のセマンティクス差異
- **[I-18]** re-export 残課題 — 予約語モジュール名のみ

### 未対応構文（ベンチマーク外）

- **[I-249]** symbol キーワード型
- **[I-250]** keyof/unique 型演算子
- **[I-251]** `**` 累乗演算子 → `f64::powf()`
- **[I-252]** `??=`/`&&=`/`||=` 複合代入
- **[I-253]** console.info/debug/trace/table
- **[I-254]** 一部の Math メソッド
- **[I-197]** template literal type
- **[I-198]** ビット否定・void・delete 演算子 — delete 2 件
- **[I-199]** 型の構造的一致による推論
- **[I-228]** interface の非プロパティメンバー — 2 件
- **[I-229]** プロパティ経由の関数呼び出し — 3 件
- **[I-280]** 関数呼び出し結果の即時呼び出し — 0 件（要再検証）
- **[I-230]** 無名クラス式 — 1 件
- **[I-231]** 型位置での rest type — 2 件
- **[I-232]** for ループの複数宣言子 — 1 件
- **[I-233]** 複雑な update 式ターゲット — 2 件
- **[I-234]** アロー関数の配列デストラクチャリング引数 — 1 件
- **[I-235]** 複数スプレッドで未登録型のフィールド展開 — 要再検証
- **[I-366]** **unregistered type path で同名 explicit キーの重複初期化** — `{ ...base, x: f(), x: g() }` のように spread 後に同じキーの explicit が複数ある場合、`convert_object_lit` の unregistered path（`src/transformer/expressions/data_literals.rs:390-398`）は spread 後の全 explicit を fields に含めるため、`StructInit { fields: [("x", f()), ("x", g())], base: Some(base) }` が生成される。Rust では同名フィールドの重複初期化はコンパイルエラー（`E0062: field 'x' specified more than once`）。TypeScript では後のキーが前のキーを上書きし、前のキーの式も評価される（副作用保持）。解決策: unregistered path でも rightmost-wins をキー単位で適用し、上書きされた式は `let _ = expr;` で副作用保持する。registered type path の rightmost-wins + `used_indices` パターンと同じアプローチ。ESLint `no-dupe-keys` 違反のため実コードでの発生確率は極めて低い

### その他

- **[I-27]** タプルの optional 要素
- **[I-29]** ネスト async 関数
- **[I-33]** 非プリミティブ type assertion のコメント保持
- **[I-34]** Math.random() → rand クレート — 🔗 I-30 が前提
- **[I-39]** Range 生成で Expr::IntLit を使用
- **[I-59]** Pick/Omit のネストしたユーティリティ型
- **[I-99]** collect_du_field_accesses_from_expr の AST ノード網羅性
- **[I-105]** デコレータ付き TsParamProp
- **[I-106]** ラベル付き while での条件内代入
- **[I-126]** 計算プロパティアクセス残存 — 0 件（要再検証）
- **[I-138]** タグ付きテンプレートリテラル — 1 件
- **[I-139]** 関数型パラメータパターン — 1 件
- **[I-148]** for...of デストラクチャリング残存 — 1 件
- **[I-151]** 代入ターゲットパターン — 2 件
- **[I-154]** カンマ式 (SeqExpr) — 1 件
- **[I-155]** satisfies 式 — 1 件
- **[I-156]** オブジェクトリテラルのゲッター/メソッド記法 — 2 件
- **[I-184]** fs モジュールの追加メソッド
- **[I-185]** process の追加プロパティ
- **[I-191]** README.md の記述見直し
- **[I-240]** 代入ターゲット（非パターン）— 1 件
- **[I-352]** クラスの computed method name 未対応 — `src/transformer/classes/members.rs:238`。1 件

### E2E テスト拡充

- **[I-20]** discriminated union の拡張パターン
- **[I-71]** E2E: オブジェクト操作
- **[I-72]** E2E: 高度なクラス機能
- **[I-74]** E2E: 既存スクリプトのエッジケース

---

## 保留

- **[I-32]** コンパイルテストのスキップ解消 — 各原因の修正が前提（23 件）
- **[I-183]** HTTP E2E テスト基盤 + fetch 変換 — I-30 が前提

## スコープ外

- heterogeneous enum — TS でも非推奨パターン
- Docker ベースの開発環境 — 複数人開発時に再評価
- Watch モード — 変換機能が十分に揃った後
- カスタム変換ルールの注入 — 具体的な需要なし
- **[I-167]** `==`（抽象等価）— 同一型なら `===` と同じ、異なる型なら Rust が型エラーで検出
- **[I-219]** conditional type — Hono 0 件。需要発生時に再評価

---

## ユーザー記載アイディア

- TypeScript, crate などのバージョンアップ
- package.json に TypeScript のバージョン指定を入れる
- 外部型生成ツールの Docker 化
- node_modules の依存を解析して全トランスパイル
- cargo nextest 導入検討
- テスト実行環境の改修、高速化
- コンパイルテストのビルトインなしの必要性検証
