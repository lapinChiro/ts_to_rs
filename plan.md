# ts_to_rs 開発計画

PRD 化済みタスクの消化順序。次のタスクから順に着手する。

## 次のタスク

- `backlog/string-method-improvements.md` (I-186, I-19) — split 型不一致修正 + substring/slice 変換

## キュー

- `backlog/trait-parameter-types.md` (I-187) — trait 型の `&dyn Trait` / `Box<dyn Trait>` 変換
- `backlog/object-literal-improvements.md` (I-112b, I-134) — オブジェクトリテラル型解決 + 計算キー（80 件）
- `backlog/type-alias-patterns.md` (I-25, I-37) — 条件型 + keyof typeof + 型リテラルメソッド（50 件）
- `backlog/small-fixes-batch.md` (I-180, I-146, I-15) — トップレベル式文 + デフォルト値 + 単項プラス

## 保留中

（なし）
